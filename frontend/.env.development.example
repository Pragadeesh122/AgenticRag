# Dev-only: point browser API calls at the local FastAPI server.
# In production the frontend is served behind an ingress that routes
# API paths to the backend, so same-origin ("") is correct.
NEXT_PUBLIC_API_URL=http://localhost:8000
